# Runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# Install dev/test extras with:  pip install -e ".[dev]"
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
structlog>=24.1.0
python-dotenv>=1.0.0
requests>=2.32.0
beautifulsoup4>=4.13.0
python-docx>=1.2.0
pymupdf>=1.23.0
pdfminer.six>=20221105
# LLM provider SDKs
anthropic>=0.93.0
openai>=1.40.0
google-genai>=0.3.0
groq>=0.11.0
# RAG embedding cache (Postgres + pgvector extension); inert unless DATABASE_URL is set
psycopg[binary]>=3.1
# NOTE: PDF export uses LibreOffice headless (system package: libreoffice-writer),
# installed in the Docker image — not a pip dependency. docx2pdf was removed
# because it requires Microsoft Word and does not run on Linux.
