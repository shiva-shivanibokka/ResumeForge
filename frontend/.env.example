# Base URL of the ResumeForge backend API.
# Dev: leave unset to use the Vite proxy (/api -> http://localhost:8000).
# Prod: set to your deployed backend, e.g. https://resumeforge-api.onrender.com
VITE_API_URL=
